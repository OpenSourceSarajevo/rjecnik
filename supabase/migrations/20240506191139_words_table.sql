CREATE TABLE words (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  word VARCHAR,
  meaning VARCHAR,
  gender VARCHAR,
  type VARCHAR,
  origin VARCHAR ARRAY,
  forms JSON ARRAY,
  alternative VARCHAR ARRAY,
  descriptors VARCHAR
);